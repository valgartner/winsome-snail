---
import BaseHead from '../../components/BaseHead.astro';
import { SITE_TITLE } from '../../consts';
import productData from '../../content/data/main.json';

const { product } = productData;
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead 
			title={`Buy ${product.name} | ${SITE_TITLE}`} 
			description="Purchase your very own Plumbus - Galactic Federation ID required"
		/>
		<style>
			:root {
				--primary: #2a623d;
				--primary-dark: #1a472a;
				--secondary: #97ce4c;
				--text: #e0e0e0;
				--background: #0d1f13;
				--accent: #44281d;
				--glow: rgba(151, 206, 76, 0.3);
			}

			body {
				margin: 0;
				font-family: 'Atkinson', sans-serif;
				background: var(--background);
				color: var(--text);
				min-height: 100vh;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
			}

			.container {
				max-width: 800px;
				margin: 0 auto;
				padding: 2rem;
				text-align: center;
				background: rgba(26, 71, 42, 0.3);
				border-radius: 20px;
				backdrop-filter: blur(10px);
				border: 1px solid rgba(151, 206, 76, 0.2);
				box-shadow: 
					0 0 20px var(--glow),
					0 0 40px var(--glow);
			}

			h1 {
				color: var(--secondary);
				font-size: clamp(2rem, 5vw, 4rem);
				margin-bottom: 2rem;
				text-shadow: 
					0 0 10px var(--glow),
					0 0 20px var(--glow);
			}

			.federation-logo {
				width: 150px;
				height: 150px;
				margin: 2rem auto;
				background: url('/Galactic_Federation.png') center/contain no-repeat;
				opacity: 0.8;
				filter: drop-shadow(0 0 10px var(--glow));
			}

			.message {
				font-size: 1.2rem;
				line-height: 1.6;
				margin-bottom: 2rem;
				padding: 1rem;
				background: rgba(151, 206, 76, 0.1);
				border-radius: 10px;
			}

			.warning {
				color: #ff6b6b;
				font-style: italic;
				font-size: 0.9rem;
				margin-top: 2rem;
			}

			.back-link {
				display: inline-block;
				margin-top: 2rem;
				padding: 1rem 2rem;
				background: var(--secondary);
				color: var(--primary-dark);
				text-decoration: none;
				border-radius: 50px;
				font-weight: bold;
				transition: all 0.3s ease;
			}

			.back-link:hover {
				transform: translateY(-3px);
				box-shadow: 0 5px 15px rgba(151, 206, 76, 0.3);
			}

			@media (max-width: 768px) {
				.container {
					margin: 1rem;
					padding: 1rem;
				}

				h1 {
					font-size: 2rem;
				}
			}
		</style>
	</head>
	<body>
		<div class="container">
			<h1>Purchase a Plumbus</h1>
			<div class="federation-logo"></div>
			<div class="message">
				<p>⚠️ ATTENTION INTERDIMENSIONAL SHOPPERS ⚠️</p>
				<p>
					To purchase a Plumbus, you must present a valid Galactic Federation identification card.
					This requirement is strictly enforced across all dimensions to prevent unauthorized Plumbus distribution.
				</p>
				<p>
					Your Galactic Federation ID must be:
				</p>
				<ul style="text-align: left; list-style-position: inside;">
					<li>Current and non-expired</li>
					<li>Properly certified by the Council of Ricks</li>
					<li>Encoded with quantum-crystalline verification</li>
					<li>Free from temporal paradox stamps</li>
				</ul>
			</div>
			<p class="warning">
				* Attempting to purchase a Plumbus without proper identification may result in immediate reporting to the nearest Galactic Federation outpost.
			</p>
			<a href="/" class="back-link">Return to Homepage</a>
		</div>
	</body>
</html>